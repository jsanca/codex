package codex.codex.api.model.value;

/**
 * Defines the editorial state of a content revision.
 */
public enum EditorialState {
    /** draft status. */
    DRAFT,
    /** live status. */
    LIVE,
    /** archived status. */
    ARCHIVED
}
